# docs/

여기에 `demo.png`를 넣으면 최상위 README의 데모 이미지 자리(`![demo](docs/demo.png)`)에 표시된다.

**캡처 방법**: 터미널에서 `python detect.py samples/` 를 실행한 뒤 rich 표 출력을 캡처해 `docs/demo.png` 로 저장.

임팩트를 키우려면 before/after 를 나란히: 은닉 텍스트가 있는 문서를 워드/뷰어로 연 화면(안 보임)과 탐지기 출력(잡아냄)을 한 장에 담는다.
